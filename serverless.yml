service: users-api

provider:
  name: aws
  runtime: nodejs20.x
  region: ap-southeast-2

functions:
  app:
    handler: server.handler
    events:
      - httpApi:
          path: /
          method: ANY
      - httpApi:
          path: /{proxy+}
          method: ANY